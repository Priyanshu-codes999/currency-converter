import { CurrencyConverter } from "@/components/CurrencyConverter";

const Index = () => {
  return (
    <main>
      <h1 className="sr-only">Flux — Beautiful Live Currency Converter</h1>
      <CurrencyConverter />
    </main>
  );
};

export default Index;
